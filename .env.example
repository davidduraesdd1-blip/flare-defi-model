# ══════════════════════════════════════════════════════════════════════════════
# DeFi Model — Environment Variables
# Copy this file to .env and fill in your keys. Never commit .env to git.
# ══════════════════════════════════════════════════════════════════════════════

# ── AI (unlocks intent classification, ecosystem digest, Intelligence tab AI) ─
ANTHROPIC_API_KEY=sk-ant-api03-...          # console.anthropic.com → API Keys
ANTHROPIC_ENABLED=true                      # set to "false" to disable all AI features without removing key

# ── Market Data ───────────────────────────────────────────────────────────────
DEFI_COINGECKO_API_KEY=CG-...              # coingecko.com/en/api — higher rate limits
DEFI_COINMETRICS_API_KEY=...               # coinmetrics.io/community (free) — BTC MVRV/SOPR; fixes 403 on Streamlit Cloud

# ── Wallet & Portfolio ────────────────────────────────────────────────────────
ZERION_API_KEY=...                         # zerion.io/developers — cross-chain wallet positions

# ── Alerts & Notifications ────────────────────────────────────────────────────
# Configure email (SMTP) and/or a generic HTTPS webhook via the Settings tab in
# the UI (stored in data/alerts_config.json — gitignored).

# ── On-Chain Execution (AgentKit) ─────────────────────────────────────────────
CDP_API_KEY_NAME=...                       # portal.cdp.coinbase.com (free) — AgentKit wallet section
CDP_API_KEY_PRIVATE_KEY=...
AGENTKIT_NETWORK=base-mainnet              # default: base-mainnet

# ── Error Monitoring ──────────────────────────────────────────────────────────
DEFI_SENTRY_DSN=https://...@sentry.io/... # sentry.io (free tier)

# ── Scheduler ─────────────────────────────────────────────────────────────────
SCHEDULER_TZ=America/Denver               # default timezone for scheduled scans

# ── Phase 2+ Live Execution (Flare chain) ─────────────────────────────────────
# Verify EVERY address against official protocol docs before live execution.
# Wrong addresses = lost funds. All empty by default = executor blocks with
# clear error before signing.
BLAZESWAP_ROUTER_ADDRESS=                 # app.blazeswap.xyz / GitHub blazeswap
SPARKDEX_ROUTER_ADDRESS=                  # docs.sparkdex.ai (Uniswap V3 fork)
SPARKDEX_QUOTER_ADDRESS=                  # Phase 3D-12: on-chain V3 Quoter for
                                          # MEV-safe amountOutMinimum (removes
                                          # off-chain price dependency).

# ── Phase 4A Operating Mode ───────────────────────────────────────────────────
# One of: PAPER / LIVE_PHASE2 / LIVE_PHASE3. Default PAPER.
# LIVE_PHASE2 requires 14 paper-trading days logged + manual unlock from UI.
# LIVE_PHASE3 unlocks higher wallet caps — additional approval required.
OPERATING_MODE=PAPER
